const firstName = 'priya';
const lastName ='nusrat jahan';
const fullName = firstName + lastName;
const fullName2 = `${firstName} ${40-50+90} is a good girl`

console.log(fullName2);

const multiLine = `i love my country. 
its a rivarine country`
console.log(multiLine);